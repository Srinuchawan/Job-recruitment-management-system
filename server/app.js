const e=require('express'),cors=require('cors');const app=e();app.use(cors({origin:process.env.CLIENT_URL||'http://localhost:5173'}));app.use(e.json({limit:'1mb'}));app.use('/uploads',e.static('uploads'));app.get('/api/health',(q,r)=>r.json({success:true,message:'Job Recruitment API is running'}));app.use('/api/auth',require('./routes/authRoutes'));app.use('/api/users',require('./routes/userRoutes'));app.use('/api/jobs',require('./routes/jobRoutes'));app.use('/api/applications',require('./routes/applicationRoutes'));app.use('/api/saved-jobs',require('./routes/savedJobRoutes'));app.use('/api/recruiter',require('./routes/recruiterRoutes'));app.use(require('./middleware/errorHandler').notFound);app.use(require('./middleware/errorHandler').errorHandler);module.exports=app;
