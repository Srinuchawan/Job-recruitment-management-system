const r=require('express').Router(),c=require('../controllers/authController'),p=require('../middleware/authMiddleware');r.post('/register',c.register);r.post('/login',c.login);r.get('/me',p,c.me);module.exports=r;
