const r=require('express').Router(),m=require('multer'),path=require('path'),fs=require('fs'),p=require('../middleware/authMiddleware'),c=require('../controllers/userController');const d=path.join(__dirname,'..',process.env.UPLOAD_DIR||'uploads');fs.mkdirSync(d,{recursive:true});const u=m({dest:d,limits:{fileSize:5*1024*1024},fileFilter:(q,f,cb)=>f.mimetype==='application/pdf'?cb(null,true):cb(new Error('Only PDF resumes are accepted.'))});r.get('/profile',p,c.profile);r.put('/profile',p,c.update);r.put('/resume',p,u.single('resume'),c.resume);module.exports=r;
