const r=require('express').Router(),p=require('../middleware/authMiddleware'),a=require('../middleware/roleMiddleware'),c=require('../controllers/recruiterController');r.use(p,a('recruiter'));r.get('/dashboard',c.dashboard);r.get('/jobs',c.jobs);r.get('/jobs/:jobId/applicants',c.applicants);r.put('/applications/:id/status',c.status);r.get('/profile',c.company);r.put('/profile',c.updateCompany);module.exports=r;
