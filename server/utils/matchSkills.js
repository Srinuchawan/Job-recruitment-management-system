const n=s=>String(s||'').toLowerCase().replace(/[.#/+_-]/g,' ').replace(/[^a-z0-9 ]/g,'').replace(/\s+/g,' ').trim();module.exports=(candidate=[],required=[])=>{const c=candidate.map(n),r=[...new Set(required.map(n).filter(Boolean))],m=r.filter(x=>c.some(y=>y===x||y.includes(x)||x.includes(y))),missing=r.filter(x=>!m.includes(x));return{score:r.length?Math.round(m.length/r.length*100):0,matchingSkills:m,missingSkills:missing}};
